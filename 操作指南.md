# 个人技术知识库操作指南

本指南详细说明如何使用基于 Obsidian 和大模型 API 搭建的个人在线技术知识库。

## 1. 本地管理

### 1.1 打开 Obsidian

1. 下载并安装 Obsidian：[Obsidian 官网](https://obsidian.md/)
2. 打开 Obsidian，选择「打开文件夹」
3. 选择 `knowledgebase` 目录作为 Obsidian 仓库

### 1.2 安装插件

在 Obsidian 中安装以下插件：

1. **OpenAI Plugin**：接入 OpenAI API
2. **Claude Plugin**：接入 Claude API
3. **Templater**：模板管理
4. **Dataview**：数据查询
5. **Git**：版本控制

安装方法：
- 打开 Obsidian 设置
- 点击「社区插件」
- 搜索并安装所需插件
- 启用插件

### 1.3 配置大模型 API

#### OpenAI API 配置

1. 获取 OpenAI API Key：
   - 访问 [OpenAI 官网](https://platform.openai.com/)
   - 登录并进入「API Keys」
   - 创建新的 API Key

2. 在 Obsidian 中配置：
   - 打开 OpenAI Plugin 设置
   - 粘贴 API Key
   - 选择模型（如 gpt-4、gpt-3.5-turbo）
   - 设置温度、最大 tokens 等参数

#### Claude API 配置

1. 获取 Claude API Key：
   - 访问 [Anthropic 官网](https://console.anthropic.com/)
   - 登录并进入「API Keys」
   - 创建新的 API Key

2. 在 Obsidian 中配置：
   - 打开 Claude Plugin 设置
   - 粘贴 API Key
   - 选择 Claude 模型版本

### 1.4 管理笔记

#### 创建笔记

1. 在 `content` 目录中创建 Markdown 文件
2. 使用 YAML frontmatter 添加元数据：
   ```yaml
   ---
   title: 笔记标题
   date: 2026-04-10
   tags: [标签1, 标签2]
   ---
   ```
3. 编写 Markdown 内容

#### 组织笔记

- **分类**：按技术领域、项目等创建子目录
- **标签**：使用标签进行快速筛选
- **链接**：使用 Obsidian 的双向链接功能
- **搜索**：使用 Obsidian 的搜索功能

## 2. 在线发布

### 2.1 构建静态网站

1. 打开终端，进入 `knowledgebase` 目录
2. 运行构建脚本：
   ```bash
   node build.js
   ```
3. 构建完成后，静态网站会生成在 `public` 目录中

### 2.2 部署网站

#### 部署到 GitHub Pages

1. 创建 GitHub 仓库
2. 将 `public` 目录部署到 GitHub Pages：
   - 方法 1：直接将 `public` 目录推送到 GitHub
   - 方法 2：使用 GitHub Actions 自动构建和部署

#### 部署到 Vercel 或 Netlify

1. 登录 Vercel 或 Netlify
2. 导入 `knowledgebase` 仓库
3. 配置构建命令：`node build.js`
4. 配置输出目录：`public`
5. 部署网站

### 2.3 自定义域名

1. 在 DNS 提供商处配置域名解析
2. 在部署平台（GitHub Pages、Vercel、Netlify）中绑定自定义域名

## 3. 功能使用

### 3.1 搜索功能

在生成的静态网站中：
1. 在搜索框中输入关键词
2. 网站会实时过滤包含关键词的笔记
3. 搜索范围包括：笔记标题、内容和标签

### 3.2 标签筛选功能

在生成的静态网站中：
1. 点击标签筛选区域的标签
2. 网站会显示包含该标签的所有笔记
3. 点击「全部」标签可查看所有笔记

### 3.3 大模型 API 使用

#### 内容生成

1. 在 Obsidian 中打开笔记
2. 使用 OpenAI 或 Claude 插件的命令生成内容：
   - 生成摘要
   - 扩展思路
   - 生成文章大纲

#### 智能问答

1. 在 Obsidian 中选择笔记内容
2. 使用插件命令进行提问
3. 大模型会基于选择的内容提供回答

#### 知识关联

1. 使用插件分析笔记内容
2. 自动发现笔记之间的关联关系
3. 生成知识图谱或关联建议

## 4. 版本控制

### 4.1 Git 操作

1. 初始化 Git 仓库（如果尚未初始化）：
   ```bash
   git init
   ```

2. 提交更改：
   ```bash
   git add .
   git commit -m "更新笔记"
   git push
   ```

3. 使用 Obsidian Git 插件：
   - 配置插件设置
   - 使用插件命令进行 Git 操作
   - 设置自动提交和推送

### 4.2 分支管理

- 创建功能分支：
  ```bash
  git checkout -b feature/新功能
  ```

- 合并分支：
  ```bash
  git checkout main
  git merge feature/新功能
  ```

## 5. 安全考虑

### 5.1 API Key 保护

- 不要在笔记中直接存储 API Key
- 使用 Obsidian 插件的安全存储功能
- 定期更新 API Key

### 5.2 数据隐私

- 注意大模型可能会处理和存储你的笔记内容
- 不要在笔记中包含敏感信息
- 了解大模型提供商的隐私政策

### 5.3 成本控制

- 设置 API 使用限额
- 监控 API 使用情况
- 选择合适的模型和参数

## 6. 优化和扩展

### 6.1 性能优化

- 压缩静态网站文件
- 使用 CDN 加速
- 优化图片和资源

### 6.2 功能扩展

- 添加更多 AI 功能
- 集成其他工具和服务
- 构建个性化推荐系统

### 6.3 内容管理

- 建立统一的笔记结构
- 使用模板提高效率
- 定期整理和更新内容

## 7. 故障排除

### 7.1 构建失败

- 检查 Markdown 文件格式
- 确保依赖已安装：`npm install`
- 检查构建脚本错误信息

### 7.2 插件问题

- 确保插件版本兼容
- 检查 API Key 配置
- 查看插件日志

### 7.3 部署问题

- 检查部署平台配置
- 确保构建输出目录正确
- 查看部署日志

## 8. 后续计划

1. **完善静态网站功能**：添加更多交互功能和主题选项
2. **集成更多 AI 功能**：如自动摘要、知识图谱生成等
3. **优化网站性能**：添加缓存、压缩等优化
4. **扩展知识库内容**：添加更多技术笔记和资源
5. **构建移动应用**：开发配套的移动应用

## 9. 参考资源

- [Obsidian 官方文档](https://help.obsidian.md/)
- [OpenAI API 文档](https://platform.openai.com/docs/)
- [Anthropic API 文档](https://docs.anthropic.com/)
- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [Vercel 文档](https://vercel.com/docs)
- [Netlify 文档](https://docs.netlify.com/)
